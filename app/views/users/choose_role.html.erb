<div class="row top-login">
  <div class="col-md-8 col-xs-10 col-md-offset-2 col-xs-offset-1">
    <%= form_tag set_role_path, method: :post, id: 'select-user' do %>
    <h3 class="title-login">BIENVENIDO</h3>
    <!-- CORREO -->
    <div class="text-left field">
      <p class="role-message">Selecciona el Rol con que deseas ingresar</p>
      <%= select_tag :custom_role_id, options_for_select(@roles.collect{ |role| [role.try(:role).try(:name).to_s+' '+((role.manager? or role.seller?) ? role.try(:sale_channel).try(:name).to_s : '' ), role.id] },@roles.try(:first).try(:id)), autofocus: true, class: 'inlineb red-text', required: true %>
    </div>
    <div class="actions top-separation-login">
      <button type="submit" class="btn btn-flat-miele btn-big">Ingresar</button>
    </div>
    <% end %>
    <p class="margin-logout top-link-back"><%= link_to '<i class="fa fa-angle-left"></i> Volver al inicio de sesión'.html_safe, destroy_user_session_path, method: :delete, class: 'sign-out' %></p>
  </div>
</div>

<script>
  $("#custom_role_id").select2();
</script>