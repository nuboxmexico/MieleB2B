$("#accordion").html('<%= j render partial: "quotations/list_quotations" %>');
$(".loader-page").fadeOut("slow");

$(document).ready( function() {
	$('.pagination a').each( function() {
		$(this).attr( 'data-remote', 'true' );
		$(this).attr('onclick', '$(".loader-page").fadeIn();');
	}) 
})